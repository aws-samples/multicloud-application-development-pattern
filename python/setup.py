from setuptools import setup

VERSION = '0.2'
DESCRIPTION = 'Multi cloud architecture core code'

setup(
    name="mcacore",
    version=VERSION,
    author="chrjonb",
    author_email="chrjonb@amazon.com",
    description=DESCRIPTION,
    packages=['mcacore'],
)